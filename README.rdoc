= cwa_as Cluster Web Access Account Signup

This plugin will provide a means for new users, after Single-Sign-On auth, to

# Agree to terms-of-service (configurable via wiki)
# Agree to system access agreement (configurable via wiki)
# Grab Single-Sign-On password to generate JSON-RPC request to create a new IPA user
# Provide for account "deletion" with terms-of-discontinuation-of-service (wiki)
